
&НаКлиенте
Процедура ПоказатьОтбор(Команда)
	Элементы.Отбор.Видимость = НЕ Элементы.Отбор.Видимость;
	Элементы.ФормаПоказатьОтбор.Пометка = Элементы.Отбор.Видимость;
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСообщение(Команда)
	Элементы.ТекстСообщения.Видимость = НЕ Элементы.ТекстСообщения.Видимость;
	Элементы.ФормаПоказатьСообщение.Пометка = Элементы.ТекстСообщения.Видимость;
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
КонецПроцедуры
